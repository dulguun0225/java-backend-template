/**
 * The worked-example feature slice: one table, one Flyway migration, generated jOOQ classes, a service that
 * goes through {@code Tx}, a controller that maps outcomes to RFC 9457 codes from a compile-checked catalog,
 * and an integration test against a real PostgreSQL. Copy its shape for every real feature, then delete it.
 */
@org.jspecify.annotations.NullMarked
package com.example.starter.greeting;
