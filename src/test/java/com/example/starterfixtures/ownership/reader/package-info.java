/**
 * Fixture feature {@code reader} for {@code TableOwnershipTest}: it owns no table, so its read of {@code GREETING}
 * is not reported and its write is.
 */
@org.jspecify.annotations.NullMarked
package com.example.starterfixtures.ownership.reader;
