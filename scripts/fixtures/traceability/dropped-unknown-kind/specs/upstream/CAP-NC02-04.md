# Source document (fixture stand-in)

A copy of a document in another repository. Its requirement ids are written bare, exactly as that repository
writes them, because inside that document they are that document's own. This file is a definition source and
never a citation source: were the gate to scan it, every id below -- and the bare SC-900 in this sentence,
which nothing anywhere defines -- would be reported as a bare citation this repo has to answer for.

## Requirements

- **FR-002**: The source document's own FR-002.
- **FR-034a**: The source document's own FR-034a.
